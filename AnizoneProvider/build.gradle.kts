// use an integer for version numbers
version = 4

cloudstream {
    description = "AniZone.to - anime subtitle dan multi-dubbing"
    language = "id"
    authors = listOf("BetbetMiro")
    status = 1
    tvTypes = listOf("Anime")

    // Menggunakan API Google agar format gambar dikonversi menjadi PNG yang valid
    iconUrl = "https://www.google.com/s2/favicons?domain=anizone.to&sz=256"
}